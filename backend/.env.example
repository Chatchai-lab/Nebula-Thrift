# Environment: "local" or "cloud"
NEBULA_ENV=local

# Local development: AWS profile name (from ~/.aws/credentials)
AWS_PROFILE=nebula-thrift

# Cloud deployment: Azure Key Vault URL
# AZURE_KEY_VAULT_URL=https://nebula-thrift-kv.vault.azure.net
